export default function desactiveBtn() {
    languageButtons.style.opacity = 0.5;
    sbmt.style.opacity = 0.5;
    btnShow.style.opacity = 0.5;
    btnTextAdding.style.opacity = 0.5;
    beginButton.style.opacity = 0.5;
}

import {languageButtons} from './../allElements';
import {sbmt} from './../allElements';
import {btnShow} from './../allElements';
import {btnTextAdding} from './../allElements';
import {beginButton} from './../allElements';
